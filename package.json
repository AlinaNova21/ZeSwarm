{
  "name": "screepsbot-zeswarm",
  "version": "1.1.0",
  "description": "",
  "main": "src/main.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp",
    "push:test": "rollup -c && yarn run push --server test",
    "push:splus1": "rollup -c && yarn run push --server splus",
    "push:splus2": "rollup -c && yarn run push --server splus2",
    "push:fordo": "rollup -c && yarn run push --server fordo",
    "push:prtest": "rollup -c && yarn run push --server prtest",
    "push:all": "rollup -c && yarn run push --server test && yarn run push --server splus && yarn run push --server splus2 && yarn run push --server fordo",
    "push": "screeps-api -b ZeSwarm_v1.1 upload dist/main.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ags131/screeps.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ags131/screeps/issues"
  },
  "homepage": "https://github.com/ags131/screeps#readme",
  "dependencies": {
    "rollup": "^1.17.0",
    "rollup-plugin-commonjs": "^10.0.1",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-root-import": "^0.2.4"
  },
  "screeps_bot": true,
  "devDependencies": {
    "chalk": "^2.4.2",
    "screeps-api": "^1.9.0",
    "standard": "^13.0.2"
  }
}
